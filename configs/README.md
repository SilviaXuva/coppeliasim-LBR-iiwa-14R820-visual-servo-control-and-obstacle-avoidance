# Configs

Versioned configuration files organized by domain.

Rules:
- application config is separate from experiment config
- scenario config is separate from controller config
- partial configs must be merged and validated
- config files must not encode architectural coupling
