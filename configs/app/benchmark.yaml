app:
  mode: benchmark
  use_case: benchmark_controllers

runtime:
  dt: 0.05
  max_steps: 100
  save_runtime_series: false

experiment:
  name: benchmark_pd_vs_adaptive
  seed: 321
  tags: [benchmark, controllers]
  notes: "Default benchmark app configuration."

scenario:
  name: controller_benchmark

results:
  base_dir: experiments/runs
