app:
  mode: experiment
  use_case: run_pbvs_with_avoidance

runtime:
  dt: 0.05
  max_steps: 120
  save_runtime_series: true

experiment:
  name: pbvs_with_avoidance_experiment
  seed: 123
  tags: [experiment, pbvs, avoidance]
  notes: "Default experiment app configuration."

scenario:
  name: person_in_workspace

results:
  base_dir: experiments/runs
