app:
  mode: ros2
  use_case: ros2_runtime

runtime:
  dt: 0.05
  max_steps: 100
  save_runtime_series: false

experiment:
  name: ros2_bridge_runtime
  seed: 7
  tags: [ros2, bridge]
  notes: "Default ROS 2 app configuration."

scenario:
  name: ros2_workspace

results:
  base_dir: experiments/runs
